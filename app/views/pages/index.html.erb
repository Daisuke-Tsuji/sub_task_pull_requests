<% if user_signed_in? %>
  <% if @feed_items.any? %>
    <ol class="microposts">
      <%= render @feed_items %>
    </ol>
  <% end %>
<% else %>
  <% @user.each do |user| %>
    <%= "id: #{user.id} name: #{user.user_name}" %>
  <% end %>
<% end %>
