<% if user_signed_in? %>
  <% if @feed_items.any? %>
      <%= render @feed_items %>
  <% end %>
<% else %>
  <% @user.each do |user| %>
    <%= "id: #{user.id} name: #{user.user_name}" %>
  <% end %>
<% end %>
